/**
 * Home sections (TESIS, ECOSISTEMA, etc.) align with the desktop header shell:
 * `max-w-[1280px]` + `px-8` inner nav — here mirrored as `px-6 md:px-8`.
 */
export const homeSectionContainerClass =
  "mx-auto w-full max-w-[1280px] px-6 md:px-8";

/** Shared eyebrow: same size, weight, tracking, and accent as across the home. */
export const homeSectionKickerClass =
  "mb-6 text-left text-xs font-medium uppercase tracking-widest text-[#4ADE80]";
